# Invoice Generator (International + Tailwind)

## Run
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
npm run preview
```

Tailwind is configured via `tailwind.config.js` and `postcss.config.js`. CSS entry is `src/index.css`.
